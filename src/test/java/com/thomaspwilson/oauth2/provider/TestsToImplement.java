package com.thomaspwilson.oauth2.provider;

public class TestsToImplement {
    // - Ensure that we can disable global support for in-body client secret
    // - Ensure that we can enable enable support for in-body client secret
    // - Ensure that we can disable per-client support for in-body client secret
    // - Ensure that we can enable per-client support for in-body client secret
    // - Ensure that we can disable refresh token reuse
    // - Ensure that we can enable refresh token reuse
    // - Ensure that we can disable refresh token issuance
    // - Ensure that we can enable refresh token issuance
    // - Ensure that we can disable permissive grant type allowance
    // - Ensure that we can enable permissive grant type allowance
    
    // - Ensure that token lifespan times work both globally and per-client
}